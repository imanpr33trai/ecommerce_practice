model Address {
  id         String   @id @map("_id")
  street     String
  city       String
  state      String
  postalCode String
  country    String
  userId     String
  createdAt  DateTime
  updatedAt  DateTime
  user       User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@map("address")
}
