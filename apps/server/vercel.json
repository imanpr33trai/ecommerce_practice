{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "version": 2,
  "installCommand": "cd ../.. && bun install",
  "buildCommand": "cd ../.. && bun run -F server build",
  "outputDirectory": "apps/server/dist",
  "framework": "hono",
  "env": {
    "NODE_ENV": "production"
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.js"
    }
  ]
}